name: Wake Up Streamlit

on:
  schedule:
    # Runs at 00:00 UTC every 3rd day
    # (e.g., 1st, 4th, 7th, 10th of the month)
    - cron: '0 0 */3 * *'
  workflow_dispatch: 

jobs:
  wake_app:
    runs-on: ubuntu-latest
    steps:
      - name: Poke App
        run: |
          # Updated to point to your new Railway App
          curl -I -L "https://q360insights.up.railway.app/"
